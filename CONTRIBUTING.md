## 📝 Соглашение об именовании коммитов и pull request'ов

Этот документ описывает формат сообщений коммитов и названий pull request'ов (PR) в проекте **PopCore**. Соблюдение этих правил помогает поддерживать чистую и понятную историю изменений.

### 🔠 Формат коммита

```
<type>(<scope>): <short summary>
```

- **`<type>`** — категория изменений (обязательно).
- **`(<scope>)`** — опциональный компонент системы, к которому относится изменение.
- **`<short summary>`** — краткое описание на английском языке (до 72 символов), начинающееся с глагола в повелительном наклонении (imperative mood).

#### Допустимые типы (`<type>`)

| Тип        | Описание                                                           |
| ---------- | ------------------------------------------------------------------ |
| `feat`     | Новая функциональность (для пользователя или разработчика)         |
| `fix`      | Исправление ошибки                                                 |
| `docs`     | Изменения в документации (README, комментарии, EBNF-пояснения)     |
| `style`    | Правки кода без изменения логики (форматирование, точки с запятой) |
| `refactor` | Рефакторинг кода (без новой функциональности или исправлений)      |
| `perf`     | Улучшение производительности                                       |
| `test`     | Добавление или исправление тестов                                  |
| `build`    | Изменения в системе сборки (Makefile, скрипты)                     |
| `ci`       | Обновления конфигурации CI/CD                                      |
| `chore`    | Рутинные задачи (обновление зависимостей, метаданные)              |

#### Примеры scopes (`<scope>`)

- `lexer`, `parser`, `codegen`, `preproc` — компоненты компилятора
- `ebnf`, `docs` — документация и грамматика
- `examples` — примеры кода
- `runtime` — минимальный ассемблерный рантайм
- `core` — ядро языка (типы, семантика)

#### ✅ Примеры корректных коммитов

```text
feat(parser): add support for struct field access
fix(codegen): correct stack alignment for i8 variables
docs(ebnf): clarify pointer type recursion rule
refactor(lexer): unify token handling for char and string literals
test(parser): add test case for nested if-else statements
build: add script to compile and link .prcore files
```

#### ❌ Примеры некорректных коммитов

```text
fixed bug                   # ← нет типа, нет смысла
update code                 # ← слишком расплывчато
Added new feature           # ← не imperative mood, нет типа
parser stuff                # ← нет структуры, нет смысла
```

---

### 🔀 Название Pull Request (PR)

Название PR должно соответствовать **одному основному коммиту** внутри него и следовать тому же формату:

```
<type>(<scope>): <short summary>
```

Если PR содержит несколько независимых изменений — разбейте его на несколько PR'ов.

---

### 🔄 Merge-коммиты

При слиянии (merge) ветки в `main` используется **обычный merge-commit** (не squash, не rebase).  
Сообщение merge-коммита генерируется автоматически GitHub/GitLab и имеет вид:

```
Merge branch 'feature/...' into main
```

Это позволяет сохранять полную историю разработки.

> 💡 **Разработчику**: перед созданием PR убедитесь, что история вашей ветки **чистая** (без `WIP`, `fix typo`, `oops` коммитов). При необходимости используйте `git rebase -i`.

---

### 🤖 Почему это важно?

- **Автоматизация**: Инструменты могут генерировать changelog из истории коммитов.
- **Читаемость**: Любой участник быстро поймёт, что изменилось и зачем.
- **Отладка**: `git bisect` становится эффективнее.
- **Профессионализм**: Чёткая история — признак зрелого проекта.
