# PopCore — язык системного программирования

**PopCore** — это низкоуровневый, безопасный и простой язык программирования, созданный для обучения принципам компиляции, управления памятью и разработки встраиваемых систем.

> **Внимание**: Это учебный проект. Язык находится в стадии активной разработки и не предназначен для использования в production.

## 🎯 Цели и философия

- **Образование**: Понятная архитектура компилятора от лексера до генерации кода.
- **Безопасность**: Статические проверки на этапе компиляции для предотвращения ошибок работы с памятью (аналогично Rust, но проще).
- **Контроль**: Прямой доступ к железу без накладных расходов стандартной библиотеки.
- **Минимализм**: Ядро языка достаточно мало, чтобы его можно было полностью понять и реализовать вручную.

## 📦 Основные возможности (v0)

- **Типы данных**: `i32`, `i8` (для ASCII), `string`, указатели (`*T`) и `void`.
- **Структуры**: Для создания сложных типов данных, например, AST-деревьев.
- **Управление памятью**: На начальном этапе — только стековая аллокация. В будущем — статический анализ владения (ownership) для автоматической и безопасной работы с кучей.
- **Препроцессор**: Поддержка `#define`, `#ifdef`, `#include` для модульности и условной компиляции.
- **Системные вызовы**: Через объявления `extern fn` без зависимости от libc.
- **Целевые платформы**: x86-64 (Linux, bare metal), с планами по поддержке микроконтроллеров (ARM Cortex-M) и SOM (System on Module).

## 📁 Формат файлов

- **`.pcore`**: Основные исходные файлы программы.
- **`.phead`**: Заголовочные файлы (в будущем). Сейчас модульность достигается через `#include`.

## 🛠 Архитектура компилятора

Компилятор `popcore` написан на Python и состоит из следующих этапов:

1.  **Препроцессор**: Обработка `#include`, `#define` и условных директив.
2.  **Лексер**: Преобразование текста в поток токенов.
3.  **Парсер**: Построение абстрактного синтаксического дерева (AST).
4.  **Семантический анализатор**: Проверка типов и корректности программы.
5.  **Генератор кода**: Преобразование AST в ассемблерный код (AT&T синтаксис для x86-64).

## 📜 Пример кода

```c
struct Node {
    value: i32;
    next: *Node;
}

extern fn write(fd: i32, buf: *i8, count: i32) -> i32;
extern fn exit(code: i32) -> void;

fn print_digit(d: i32) -> void {
    let c: i8 = '0' + d;
    write(1, &c, 1);
}

fn main() -> i32 {
    let a: i32 = 0;
    let b: i32 = 1;
    let i: i32 = 0;

    while i < 10 {
        // Печатаем 'a'
        print_digit(a / 10);
        print_digit(a % 10);
        write(1, "\n", 1);

        let next: i32 = a + b;
        a = b;
        b = next;
        i = i + 1;
    }

    return 0;
}
```

## 📐 Грамматика языка

Полная формальная спецификация синтаксиса PopCore содержится в следующих файлах:

- **`popcore.ebnf`** — основная грамматика языка в формате **расширенной формы Бэкуса–Наура (EBNF)**.  
  Этот файл содержит полное описание как препроцессора, так и основного языка и служит эталоном для реализации лексера и парсера.

- **`ebnf_schema.md`** — пояснительный документ, который расшифровывает обозначения, используемые в `popcore.ebnf`, объясняет структуру грамматики и содержит примеры соответствия правил реальному коду.  
  Полезен для понимания логики языка и отладки парсера.

Эти файлы являются **каноническим источником истины** о синтаксисе PopCore и рекомендуются к изучению перед началом работы с компилятором или написанием программ на языке.

## 📚 Дорожная карта

- **v0.1**: Минимальный компилятор с поддержкой функций, структур и стековой памяти.
- **v0.5**: Добавление inline assembly и базовой поддержки кучи через `extern malloc`.
- **v1.0**: Самокомпиляция! Компилятор, написанный на PopCore.
- **v1.5**: Реализация системы владения (ownership) для безопасной работы с памятью.
- **v2.0**: Поддержка целевых платформ для микроконтроллеров (ARM Cortex-M).

## 📄 Лицензия

Проект распространяется под лицензией **MIT**. Подробности см. в файле [LICENSE](LICENSE).
